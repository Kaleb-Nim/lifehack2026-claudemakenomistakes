{
  "name": "lifehack2026-monorepo",
  "private": true,
  "workspaces": ["demo/*"],
  "scripts": {
    "dev:merchant": "bun run --cwd demo/merchant dev",
    "build:merchant": "bun run --cwd demo/merchant build"
  }
}
